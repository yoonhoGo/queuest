# Queuest 작업 지침

## 디자인 일관성

새 기능에 화면·컴포넌트·스타일·사용자 상호작용이 포함되거나 기존 UI를 수정하는 경우, **구현 전에 [디자인 시스템](docs/design-system.md)을 반드시 읽고 따른다.** [DESIGN.md](DESIGN.md)는 해당 문서의 진입점이다. 순수 백엔드 작업에는 불필요한 UI 변경을 추가하지 않는다.

- 승인된 시각적 기준은 `previews/retro/dist/`의 웹 미리보기다. 기존 앱 화면과 다를 때에는 디자인 문서의 ‘의도된 차이’를 확인하고, 그 외 차이는 임의로 정당화하지 않는다.
- `apps/desktop/src/styles/tokens.css`와 기존 공용 컴포넌트를 우선 재사용한다. 새 기능마다 별도 팔레트·폰트·버튼·카드 테마를 만들지 않는다.
- 캐릭터의 ‘완료한 퀘스트 / 완료한 스테이지’는 `CharacterCompletionStats`의 두 카드 구조를 유지한다. 실제 도메인 계산을 연결하고 미리보기 예시 숫자는 복사하지 않는다.
- 새로운 토큰·재사용 패턴·의도된 디자인 차이가 필요하면 `docs/design-system.md`와 해당 공용 코드에 함께 반영한다. 같은 규칙을 여러 MD 파일에 중복 정의하지 않는다.
- UI 변경 후 변경된 화면의 실제 동작과 반응형 상태를 확인한다. 기준 크기·한글·포커스·스크롤·원본 비교 방법은 디자인 시스템 7절을 따른다.
- 검증 결과를 보고할 때 실제로 확인한 환경을 구분한다. 브라우저 fixture를 네이티브 앱 또는 사용자 DB 검증이라고 보고하지 않는다.

## 작업 범위

사용자의 최신 지시를 우선한다. 기존 작업 중인 변경을 보존하며, 요청하지 않은 전면 리팩터링·커밋·배포를 디자인 수정에 묶지 않는다. 이 지침은 별도의 승인 절차를 추가하지 않는다.
